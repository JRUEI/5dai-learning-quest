(function(){const material=document.body.dataset.material;const button=document.querySelector("#mark-material-done");if(!button||material!=="podcast")return;const ids=CourseData.tasks.filter(task=>task.group==="podcast").map(task=>task.id);const render=()=>{const done=ids.every(id=>ProgressStore.state.done[id]);button.textContent=done?"Podcast 已完成 ✓":"標記 Podcast 已完成";button.classList.toggle("done",done)};button.addEventListener("click",()=>{ids.forEach(id=>{ProgressStore.state.done[id]=true});ProgressStore.state.done["assignment-podcast"]=true;ProgressStore.save();render()});render()})();
